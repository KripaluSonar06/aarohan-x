# Core web framework (not used directly, but useful for API if needed)
fastapi==0.115.5
uvicorn==0.32.1

# LangGraph and LangChain (for state machine and LLM tooling)
langgraph==0.2.60
langchain==0.3.13
langchain-groq==0.2.3
langsmith==0.1.147

# LLM providers
groq==0.13.0

# Data processing and ML
pandas==2.2.3
numpy==1.26.4
scikit-learn==1.5.2
xgboost==2.1.3

# Voice and audio
edge-tts==7.0.0
faster-whisper==1.0.3
# Sarvam AI (if used)
sarvam-python==0.1.0   # replace with actual package name if available

# Database
sqlalchemy==2.0.36

# Dashboard
streamlit==1.41.1
plotly==5.24.1

# Scheduler
APScheduler==3.10.4

# Utilities
python-dotenv==1.0.1
pydantic==2.10.3
python-dateutil==2.9.0.post0
requests==2.32.3
# Optional for JWT etc not needed